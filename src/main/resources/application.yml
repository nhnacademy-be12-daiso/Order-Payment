server:
  port: 8082

spring:
  application:
    name: team3-order-payment

  profiles:
    active: dev-toss          # dev-toss 프로필로 실행

  datasource:
    url: jdbc:mysql://s4.java21.net:13306/project_be12_team3?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: project_be12_team3
    password: 'Vu52gc0i(mNxDJyI'
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  rabbitmq:
    host: s4.java21.net
    port: 5672
    username: admin
    password: 'nhnacademy123!'

  data:
    redis:
      host: s4.java21.net
      port: 6379
      password: '*N2vya7H@muDTwdNMR!'
      database: 375

  sql:
    init:
      mode: always

  h2:
    console:
      enabled: true
      path: /h2-console

# -------------- payment 전용 설정 --------------
payment:
  base-url: https://api.tosspayments.com/v1/payments
  secret-key: test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    url: /v3/api-docs
  api-docs:
    enabled: true
  packages-to-scan: com.nhnacademy.order_payments.payment.controller
  paths-to-match: /api/**

# -------------- Eureka 설정 (여기가 핵심) --------------
eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      # 로컬에서 유레카 서버를 8761로 띄울 때 주소
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
