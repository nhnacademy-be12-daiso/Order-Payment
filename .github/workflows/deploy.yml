name: Order_payments - Build and Deploy

on:
  push:
    branches: [ main, feature/cicd ]
  pull_request:
    branches: [ main, feature/cicd ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build with Maven
        run: |
          chmod +x ./mvnw
          ./mvnw clean package -DskipTests

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/order_payments:latest

      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: 8822
          script: |
            set -e
            
            echo "🔄 Order_payments 배포 시작..."
            
            # 네트워크 확인 (에러 로그 방지용으로 개선)
            docker network inspect team3-network >/dev/null 2>&1 || docker network create team3-network
            
            # ★ 수정된 부분: 조건문 없이 무조건 삭제 시도
            echo "🧹 기존 컨테이너 강제 삭제 (없으면 통과)"
            docker rm -f order_payments || true
            
            echo "⬇️ 이미지 Pull"
            docker pull ${{ secrets.DOCKER_USERNAME }}/order_payments:latest
            
            echo "🚀 order_payments 실행"
            docker run -d \
              --name order_payments \
              --network team3-network \
              --restart unless-stopped \
              -e EUREKA_CLIENT_SERVICEURL_DEFAULTZONE="http://eureka:8761/eureka/" \
              ${{ secrets.DOCKER_USERNAME }}/order_payments:latest
            
            sleep 5
            docker logs --tail 20 order_payments